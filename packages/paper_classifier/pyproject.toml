[project]
name = "paper-classifier"
version = "0.1.0"
description = "A clean Mamba-based text classification system for academic papers"
requires-python = ">=3.13"
dependencies = [
    "accelerate",
    "bitsandbytes>=0.47.0",
    "duckdb>=1.3.2",
    "evaluate",
    "mamba-ssm",
    "numpy",
    "pandas>=2.3.2",
    "python-dotenv>=1.1.1",
    "pyyaml>=6.0.2",
    "scikit-learn",
    "torch",
    "wandb>=0.21.2",
    "transformers",
    "datasets",
    "huggingface-hub",
]

[project.scripts]
train-paper-classifier = "paper_classifier.main:main"
list-paper-models = "paper_classifier.model_manager:main"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"
# Configuration for 8-16k dataset with 1000 samples
# Minimal config - inherits all common settings from label_discipline_8_16k.yaml

extends: label_discipline_8_16k.yaml

subset_generation:
  sample_count: 1000
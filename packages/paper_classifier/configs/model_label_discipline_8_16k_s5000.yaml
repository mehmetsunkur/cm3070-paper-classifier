# Configuration for 8-16k dataset with 5000 samples
# Minimal config - inherits all common settings from label_discipline_8_16k.yaml

extends: label_discipline_8_16k.yaml

subset_generation:
  sample_count: 5000

# Optional: Adjust batch sizes if needed for larger sample count
# Uncomment and modify if different from the default batch_size_train: 3
# training:
#   batch_size_train: 4
#   batch_size_eval: 4
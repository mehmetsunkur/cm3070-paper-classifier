# Configuration for 32-64k dataset with 1000 samples
# Minimal config - inherits all common settings from label_discipline_32_64k.yaml

extends: label_discipline_32_64k.yaml

subset_generation:
  sample_count: 1000
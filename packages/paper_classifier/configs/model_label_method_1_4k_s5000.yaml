# Configuration for 1-4k dataset with 5000 samples
# Minimal config - inherits all common settings from label_method_1_4k.yaml

extends: label_method_1_4k.yaml

subset_generation:
  sample_count: 5000

# Optional: Adjust batch sizes if needed for larger sample count
# Uncomment and modify if different from the default batch_size_train: 20
# training:
#   batch_size_train: 20
#   batch_size_eval: 20
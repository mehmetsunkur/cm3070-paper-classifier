# Configuration for 16-32k dataset with 10000 samples
# Minimal config - inherits all common settings from label_discipline_16_32k.yaml

extends: label_discipline_16_32k.yaml

subset_generation:
  sample_count: 10000

# Optional: Adjust batch sizes if needed for larger sample count
# Uncomment and modify if different from the default batch_size_train: 2
# training:
#   batch_size_train: 2
#   batch_size_eval: 2
# Configuration for 4-8k dataset with 5000 samples
# Minimal config - inherits all common settings from label_discipline_4_8k.yaml

extends: label_discipline_4_8k.yaml

subset_generation:
  sample_count: 5000

# Optional: Adjust batch sizes if needed for larger sample count
# Uncomment and modify if different from the default batch_size_train: 2
# training:
#   batch_size_train: 4
#   batch_size_eval: 4